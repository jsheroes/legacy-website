language: node_js
node_js:
  - '10'
cache:
  directories:
    - ~/.npm
    - ~/.cache

install:
  - npm ci

addons:
  sonarcloud:
    organization: "jsheroes" # the key of the organization

script:
  - npm run linter
  - npm run build
  - npm run test:e2e:ci
  - sonar-scanner
